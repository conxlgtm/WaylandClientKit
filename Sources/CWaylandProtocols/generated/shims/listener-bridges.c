/* Generated by `swift run wck protocols generate`.
 * Sources: vendored Wayland XML and protocols/listener-bridge-policy.json.
 * Do not edit.
 */
#include "generated/shims/listener-bridges.h"
#include "generated/legacy-unstable/linux-dmabuf/linux-dmabuf-unstable-v1-client-protocol.h"
#include "generated/legacy-unstable/pointer-constraints/pointer-constraints-unstable-v1-client-protocol.h"
#include "generated/legacy-unstable/pointer-gestures/pointer-gestures-unstable-v1-client-protocol.h"
#include "generated/legacy-unstable/primary-selection/primary-selection-unstable-v1-client-protocol.h"
#include "generated/legacy-unstable/relative-pointer/relative-pointer-unstable-v1-client-protocol.h"
#include "generated/legacy-unstable/text-input/text-input-unstable-v3-client-protocol.h"
#include "generated/legacy-unstable/xdg-decoration/xdg-decoration-unstable-v1-client-protocol.h"
#include "generated/legacy-unstable/xdg-output/xdg-output-unstable-v1-client-protocol.h"
#include "generated/stable/tablet/tablet-v2-client-protocol.h"
#include "generated/stable/xdg-shell/xdg-shell-client-protocol.h"
#include "generated/staging/ext-foreign-toplevel-list/ext-foreign-toplevel-list-v1-client-protocol.h"
#include "generated/staging/fractional-scale/fractional-scale-v1-client-protocol.h"
#include "generated/staging/xdg-activation/xdg-activation-v1-client-protocol.h"
#include "generated/staging/xdg-session-management/xdg-session-management-v1-client-protocol.h"
#include "generated/staging/xdg-toplevel-icon/xdg-toplevel-icon-v1-client-protocol.h"
#include "generated/wlr-unstable/output-management/wlr-output-management-unstable-v1-client-protocol.h"

static void swl_ext_foreign_toplevel_handle_v1_handle_closed(
    void *listener_data,
    struct ext_foreign_toplevel_handle_v1 *object)
{
    const struct swl_ext_foreign_toplevel_handle_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->closed) {
        callbacks->closed(
            callbacks->data,
            object);
    }
}

static void swl_ext_foreign_toplevel_handle_v1_handle_done(
    void *listener_data,
    struct ext_foreign_toplevel_handle_v1 *object)
{
    const struct swl_ext_foreign_toplevel_handle_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->done) {
        callbacks->done(
            callbacks->data,
            object);
    }
}

static void swl_ext_foreign_toplevel_handle_v1_handle_title(
    void *listener_data,
    struct ext_foreign_toplevel_handle_v1 *object,
    const char *event_title)
{
    const struct swl_ext_foreign_toplevel_handle_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->title) {
        callbacks->title(
            callbacks->data,
            object,
            event_title);
    }
}

static void swl_ext_foreign_toplevel_handle_v1_handle_app_id(
    void *listener_data,
    struct ext_foreign_toplevel_handle_v1 *object,
    const char *event_app_id)
{
    const struct swl_ext_foreign_toplevel_handle_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->app_id) {
        callbacks->app_id(
            callbacks->data,
            object,
            event_app_id);
    }
}

static void swl_ext_foreign_toplevel_handle_v1_handle_identifier(
    void *listener_data,
    struct ext_foreign_toplevel_handle_v1 *object,
    const char *event_identifier)
{
    const struct swl_ext_foreign_toplevel_handle_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->identifier) {
        callbacks->identifier(
            callbacks->data,
            object,
            event_identifier);
    }
}

static const struct ext_foreign_toplevel_handle_v1_listener
    swl_ext_foreign_toplevel_handle_v1_listener = {
        .closed = swl_ext_foreign_toplevel_handle_v1_handle_closed,
        .done = swl_ext_foreign_toplevel_handle_v1_handle_done,
        .title = swl_ext_foreign_toplevel_handle_v1_handle_title,
        .app_id = swl_ext_foreign_toplevel_handle_v1_handle_app_id,
        .identifier = swl_ext_foreign_toplevel_handle_v1_handle_identifier,
    };

int swl_ext_foreign_toplevel_handle_v1_add_listener(
    struct ext_foreign_toplevel_handle_v1 *object,
    const struct swl_ext_foreign_toplevel_handle_v1_listener_callbacks *callbacks)
{
    return ext_foreign_toplevel_handle_v1_add_listener(
        object,
        &swl_ext_foreign_toplevel_handle_v1_listener,
        (void *)callbacks);
}

static void swl_ext_foreign_toplevel_list_v1_handle_toplevel(
    void *listener_data,
    struct ext_foreign_toplevel_list_v1 *object,
    struct ext_foreign_toplevel_handle_v1 *event_toplevel)
{
    const struct swl_ext_foreign_toplevel_list_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->toplevel) {
        callbacks->toplevel(
            callbacks->data,
            object,
            event_toplevel);
    }
}

static void swl_ext_foreign_toplevel_list_v1_handle_finished(
    void *listener_data,
    struct ext_foreign_toplevel_list_v1 *object)
{
    const struct swl_ext_foreign_toplevel_list_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->finished) {
        callbacks->finished(
            callbacks->data,
            object);
    }
}

static const struct ext_foreign_toplevel_list_v1_listener
    swl_ext_foreign_toplevel_list_v1_listener = {
        .toplevel = swl_ext_foreign_toplevel_list_v1_handle_toplevel,
        .finished = swl_ext_foreign_toplevel_list_v1_handle_finished,
    };

int swl_ext_foreign_toplevel_list_v1_add_listener(
    struct ext_foreign_toplevel_list_v1 *object,
    const struct swl_ext_foreign_toplevel_list_v1_listener_callbacks *callbacks)
{
    return ext_foreign_toplevel_list_v1_add_listener(
        object,
        &swl_ext_foreign_toplevel_list_v1_listener,
        (void *)callbacks);
}

static void swl_callback_handle_done(
    void *listener_data,
    struct wl_callback *object,
    uint32_t event_callback_data)
{
    const struct swl_callback_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->done) {
        callbacks->done(
            callbacks->data,
            object,
            event_callback_data);
    }
}

static const struct wl_callback_listener
    swl_callback_listener = {
        .done = swl_callback_handle_done,
    };

int swl_callback_add_listener(
    struct wl_callback *object,
    const struct swl_callback_listener_callbacks *callbacks)
{
    return wl_callback_add_listener(
        object,
        &swl_callback_listener,
        (void *)callbacks);
}

static void swl_data_device_handle_data_offer(
    void *listener_data,
    struct wl_data_device *object,
    struct wl_data_offer *event_id)
{
    const struct swl_data_device_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->data_offer) {
        callbacks->data_offer(
            callbacks->data,
            object,
            event_id);
    }
}

static void swl_data_device_handle_enter(
    void *listener_data,
    struct wl_data_device *object,
    uint32_t event_serial,
    struct wl_surface *event_surface,
    wl_fixed_t event_x,
    wl_fixed_t event_y,
    struct wl_data_offer *event_id)
{
    const struct swl_data_device_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->enter) {
        callbacks->enter(
            callbacks->data,
            object,
            event_serial,
            event_surface,
            event_x,
            event_y,
            event_id);
    }
}

static void swl_data_device_handle_leave(
    void *listener_data,
    struct wl_data_device *object)
{
    const struct swl_data_device_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->leave) {
        callbacks->leave(
            callbacks->data,
            object);
    }
}

static void swl_data_device_handle_motion(
    void *listener_data,
    struct wl_data_device *object,
    uint32_t event_time,
    wl_fixed_t event_x,
    wl_fixed_t event_y)
{
    const struct swl_data_device_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->motion) {
        callbacks->motion(
            callbacks->data,
            object,
            event_time,
            event_x,
            event_y);
    }
}

static void swl_data_device_handle_drop(
    void *listener_data,
    struct wl_data_device *object)
{
    const struct swl_data_device_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->drop) {
        callbacks->drop(
            callbacks->data,
            object);
    }
}

static void swl_data_device_handle_selection(
    void *listener_data,
    struct wl_data_device *object,
    struct wl_data_offer *event_id)
{
    const struct swl_data_device_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->selection) {
        callbacks->selection(
            callbacks->data,
            object,
            event_id);
    }
}

static const struct wl_data_device_listener
    swl_data_device_listener = {
        .data_offer = swl_data_device_handle_data_offer,
        .enter = swl_data_device_handle_enter,
        .leave = swl_data_device_handle_leave,
        .motion = swl_data_device_handle_motion,
        .drop = swl_data_device_handle_drop,
        .selection = swl_data_device_handle_selection,
    };

int swl_data_device_add_listener(
    struct wl_data_device *object,
    const struct swl_data_device_listener_callbacks *callbacks)
{
    return wl_data_device_add_listener(
        object,
        &swl_data_device_listener,
        (void *)callbacks);
}

static void swl_data_offer_handle_offer(
    void *listener_data,
    struct wl_data_offer *object,
    const char *event_mime_type)
{
    const struct swl_data_offer_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->offer) {
        callbacks->offer(
            callbacks->data,
            object,
            event_mime_type);
    }
}

static void swl_data_offer_handle_source_actions(
    void *listener_data,
    struct wl_data_offer *object,
    uint32_t event_source_actions)
{
    const struct swl_data_offer_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->source_actions) {
        callbacks->source_actions(
            callbacks->data,
            object,
            event_source_actions);
    }
}

static void swl_data_offer_handle_action(
    void *listener_data,
    struct wl_data_offer *object,
    uint32_t event_dnd_action)
{
    const struct swl_data_offer_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->action) {
        callbacks->action(
            callbacks->data,
            object,
            event_dnd_action);
    }
}

static const struct wl_data_offer_listener
    swl_data_offer_listener = {
        .offer = swl_data_offer_handle_offer,
        .source_actions = swl_data_offer_handle_source_actions,
        .action = swl_data_offer_handle_action,
    };

int swl_data_offer_add_listener(
    struct wl_data_offer *object,
    const struct swl_data_offer_listener_callbacks *callbacks)
{
    return wl_data_offer_add_listener(
        object,
        &swl_data_offer_listener,
        (void *)callbacks);
}

static void swl_data_source_handle_target(
    void *listener_data,
    struct wl_data_source *object,
    const char *event_mime_type)
{
    const struct swl_data_source_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->target) {
        callbacks->target(
            callbacks->data,
            object,
            event_mime_type);
    }
}

static void swl_data_source_handle_send(
    void *listener_data,
    struct wl_data_source *object,
    const char *event_mime_type,
    int32_t event_fd)
{
    const struct swl_data_source_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->send) {
        callbacks->send(
            callbacks->data,
            object,
            event_mime_type,
            event_fd);
    }
}

static void swl_data_source_handle_cancelled(
    void *listener_data,
    struct wl_data_source *object)
{
    const struct swl_data_source_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->cancelled) {
        callbacks->cancelled(
            callbacks->data,
            object);
    }
}

static void swl_data_source_handle_dnd_drop_performed(
    void *listener_data,
    struct wl_data_source *object)
{
    const struct swl_data_source_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->dnd_drop_performed) {
        callbacks->dnd_drop_performed(
            callbacks->data,
            object);
    }
}

static void swl_data_source_handle_dnd_finished(
    void *listener_data,
    struct wl_data_source *object)
{
    const struct swl_data_source_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->dnd_finished) {
        callbacks->dnd_finished(
            callbacks->data,
            object);
    }
}

static void swl_data_source_handle_action(
    void *listener_data,
    struct wl_data_source *object,
    uint32_t event_dnd_action)
{
    const struct swl_data_source_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->action) {
        callbacks->action(
            callbacks->data,
            object,
            event_dnd_action);
    }
}

static const struct wl_data_source_listener
    swl_data_source_listener = {
        .target = swl_data_source_handle_target,
        .send = swl_data_source_handle_send,
        .cancelled = swl_data_source_handle_cancelled,
        .dnd_drop_performed = swl_data_source_handle_dnd_drop_performed,
        .dnd_finished = swl_data_source_handle_dnd_finished,
        .action = swl_data_source_handle_action,
    };

int swl_data_source_add_listener(
    struct wl_data_source *object,
    const struct swl_data_source_listener_callbacks *callbacks)
{
    return wl_data_source_add_listener(
        object,
        &swl_data_source_listener,
        (void *)callbacks);
}

static void swl_keyboard_handle_keymap(
    void *listener_data,
    struct wl_keyboard *object,
    uint32_t event_format,
    int32_t event_fd,
    uint32_t event_size)
{
    const struct swl_keyboard_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->keymap) {
        callbacks->keymap(
            callbacks->data,
            object,
            event_format,
            event_fd,
            event_size);
    }
}

static void swl_keyboard_handle_enter(
    void *listener_data,
    struct wl_keyboard *object,
    uint32_t event_serial,
    struct wl_surface *event_surface,
    struct wl_array *event_keys)
{
    const struct swl_keyboard_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->enter) {
        callbacks->enter(
            callbacks->data,
            object,
            event_serial,
            event_surface,
            event_keys);
    }
}

static void swl_keyboard_handle_leave(
    void *listener_data,
    struct wl_keyboard *object,
    uint32_t event_serial,
    struct wl_surface *event_surface)
{
    const struct swl_keyboard_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->leave) {
        callbacks->leave(
            callbacks->data,
            object,
            event_serial,
            event_surface);
    }
}

static void swl_keyboard_handle_key(
    void *listener_data,
    struct wl_keyboard *object,
    uint32_t event_serial,
    uint32_t event_time,
    uint32_t event_key,
    uint32_t event_state)
{
    const struct swl_keyboard_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->key) {
        callbacks->key(
            callbacks->data,
            object,
            event_serial,
            event_time,
            event_key,
            event_state);
    }
}

static void swl_keyboard_handle_modifiers(
    void *listener_data,
    struct wl_keyboard *object,
    uint32_t event_serial,
    uint32_t event_mods_depressed,
    uint32_t event_mods_latched,
    uint32_t event_mods_locked,
    uint32_t event_group)
{
    const struct swl_keyboard_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->modifiers) {
        callbacks->modifiers(
            callbacks->data,
            object,
            event_serial,
            event_mods_depressed,
            event_mods_latched,
            event_mods_locked,
            event_group);
    }
}

static void swl_keyboard_handle_repeat_info(
    void *listener_data,
    struct wl_keyboard *object,
    int32_t event_rate,
    int32_t event_delay)
{
    const struct swl_keyboard_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->repeat_info) {
        callbacks->repeat_info(
            callbacks->data,
            object,
            event_rate,
            event_delay);
    }
}

static const struct wl_keyboard_listener
    swl_keyboard_listener = {
        .keymap = swl_keyboard_handle_keymap,
        .enter = swl_keyboard_handle_enter,
        .leave = swl_keyboard_handle_leave,
        .key = swl_keyboard_handle_key,
        .modifiers = swl_keyboard_handle_modifiers,
        .repeat_info = swl_keyboard_handle_repeat_info,
    };

int swl_keyboard_add_listener(
    struct wl_keyboard *object,
    const struct swl_keyboard_listener_callbacks *callbacks)
{
    return wl_keyboard_add_listener(
        object,
        &swl_keyboard_listener,
        (void *)callbacks);
}

static void swl_pointer_handle_enter(
    void *listener_data,
    struct wl_pointer *object,
    uint32_t event_serial,
    struct wl_surface *event_surface,
    wl_fixed_t event_surface_x,
    wl_fixed_t event_surface_y)
{
    const struct swl_pointer_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->enter) {
        callbacks->enter(
            callbacks->data,
            object,
            event_serial,
            event_surface,
            event_surface_x,
            event_surface_y);
    }
}

static void swl_pointer_handle_leave(
    void *listener_data,
    struct wl_pointer *object,
    uint32_t event_serial,
    struct wl_surface *event_surface)
{
    const struct swl_pointer_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->leave) {
        callbacks->leave(
            callbacks->data,
            object,
            event_serial,
            event_surface);
    }
}

static void swl_pointer_handle_motion(
    void *listener_data,
    struct wl_pointer *object,
    uint32_t event_time,
    wl_fixed_t event_surface_x,
    wl_fixed_t event_surface_y)
{
    const struct swl_pointer_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->motion) {
        callbacks->motion(
            callbacks->data,
            object,
            event_time,
            event_surface_x,
            event_surface_y);
    }
}

static void swl_pointer_handle_button(
    void *listener_data,
    struct wl_pointer *object,
    uint32_t event_serial,
    uint32_t event_time,
    uint32_t event_button,
    uint32_t event_state)
{
    const struct swl_pointer_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->button) {
        callbacks->button(
            callbacks->data,
            object,
            event_serial,
            event_time,
            event_button,
            event_state);
    }
}

static void swl_pointer_handle_axis(
    void *listener_data,
    struct wl_pointer *object,
    uint32_t event_time,
    uint32_t event_axis,
    wl_fixed_t event_value)
{
    const struct swl_pointer_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->axis) {
        callbacks->axis(
            callbacks->data,
            object,
            event_time,
            event_axis,
            event_value);
    }
}

static void swl_pointer_handle_frame(
    void *listener_data,
    struct wl_pointer *object)
{
    const struct swl_pointer_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->frame) {
        callbacks->frame(
            callbacks->data,
            object);
    }
}

static void swl_pointer_handle_axis_source(
    void *listener_data,
    struct wl_pointer *object,
    uint32_t event_axis_source)
{
    const struct swl_pointer_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->axis_source) {
        callbacks->axis_source(
            callbacks->data,
            object,
            event_axis_source);
    }
}

static void swl_pointer_handle_axis_stop(
    void *listener_data,
    struct wl_pointer *object,
    uint32_t event_time,
    uint32_t event_axis)
{
    const struct swl_pointer_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->axis_stop) {
        callbacks->axis_stop(
            callbacks->data,
            object,
            event_time,
            event_axis);
    }
}

static void swl_pointer_handle_axis_discrete(
    void *listener_data,
    struct wl_pointer *object,
    uint32_t event_axis,
    int32_t event_discrete)
{
    const struct swl_pointer_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->axis_discrete) {
        callbacks->axis_discrete(
            callbacks->data,
            object,
            event_axis,
            event_discrete);
    }
}

static void swl_pointer_handle_axis_value120(
    void *listener_data,
    struct wl_pointer *object,
    uint32_t event_axis,
    int32_t event_value120)
{
    const struct swl_pointer_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->axis_value120) {
        callbacks->axis_value120(
            callbacks->data,
            object,
            event_axis,
            event_value120);
    }
}

static void swl_pointer_handle_axis_relative_direction(
    void *listener_data,
    struct wl_pointer *object,
    uint32_t event_axis,
    uint32_t event_direction)
{
    const struct swl_pointer_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->axis_relative_direction) {
        callbacks->axis_relative_direction(
            callbacks->data,
            object,
            event_axis,
            event_direction);
    }
}

static const struct wl_pointer_listener
    swl_pointer_listener = {
        .enter = swl_pointer_handle_enter,
        .leave = swl_pointer_handle_leave,
        .motion = swl_pointer_handle_motion,
        .button = swl_pointer_handle_button,
        .axis = swl_pointer_handle_axis,
        .frame = swl_pointer_handle_frame,
        .axis_source = swl_pointer_handle_axis_source,
        .axis_stop = swl_pointer_handle_axis_stop,
        .axis_discrete = swl_pointer_handle_axis_discrete,
        .axis_value120 = swl_pointer_handle_axis_value120,
        .axis_relative_direction = swl_pointer_handle_axis_relative_direction,
    };

int swl_pointer_add_listener(
    struct wl_pointer *object,
    const struct swl_pointer_listener_callbacks *callbacks)
{
    return wl_pointer_add_listener(
        object,
        &swl_pointer_listener,
        (void *)callbacks);
}

static void swl_registry_handle_global(
    void *listener_data,
    struct wl_registry *object,
    uint32_t event_name,
    const char *event_interface,
    uint32_t event_version)
{
    const struct swl_registry_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->global) {
        callbacks->global(
            callbacks->data,
            object,
            event_name,
            event_interface,
            event_version);
    }
}

static void swl_registry_handle_global_remove(
    void *listener_data,
    struct wl_registry *object,
    uint32_t event_name)
{
    const struct swl_registry_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->global_remove) {
        callbacks->global_remove(
            callbacks->data,
            object,
            event_name);
    }
}

static const struct wl_registry_listener
    swl_registry_listener = {
        .global = swl_registry_handle_global,
        .global_remove = swl_registry_handle_global_remove,
    };

int swl_registry_add_listener(
    struct wl_registry *object,
    const struct swl_registry_listener_callbacks *callbacks)
{
    return wl_registry_add_listener(
        object,
        &swl_registry_listener,
        (void *)callbacks);
}

static void swl_seat_handle_capabilities(
    void *listener_data,
    struct wl_seat *object,
    uint32_t event_capabilities)
{
    const struct swl_seat_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->capabilities) {
        callbacks->capabilities(
            callbacks->data,
            object,
            event_capabilities);
    }
}

static void swl_seat_handle_name(
    void *listener_data,
    struct wl_seat *object,
    const char *event_name)
{
    const struct swl_seat_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->name) {
        callbacks->name(
            callbacks->data,
            object,
            event_name);
    }
}

static const struct wl_seat_listener
    swl_seat_listener = {
        .capabilities = swl_seat_handle_capabilities,
        .name = swl_seat_handle_name,
    };

int swl_seat_add_listener(
    struct wl_seat *object,
    const struct swl_seat_listener_callbacks *callbacks)
{
    return wl_seat_add_listener(
        object,
        &swl_seat_listener,
        (void *)callbacks);
}

static void swl_touch_handle_down(
    void *listener_data,
    struct wl_touch *object,
    uint32_t event_serial,
    uint32_t event_time,
    struct wl_surface *event_surface,
    int32_t event_id,
    wl_fixed_t event_x,
    wl_fixed_t event_y)
{
    const struct swl_touch_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->down) {
        callbacks->down(
            callbacks->data,
            object,
            event_serial,
            event_time,
            event_surface,
            event_id,
            event_x,
            event_y);
    }
}

static void swl_touch_handle_up(
    void *listener_data,
    struct wl_touch *object,
    uint32_t event_serial,
    uint32_t event_time,
    int32_t event_id)
{
    const struct swl_touch_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->up) {
        callbacks->up(
            callbacks->data,
            object,
            event_serial,
            event_time,
            event_id);
    }
}

static void swl_touch_handle_motion(
    void *listener_data,
    struct wl_touch *object,
    uint32_t event_time,
    int32_t event_id,
    wl_fixed_t event_x,
    wl_fixed_t event_y)
{
    const struct swl_touch_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->motion) {
        callbacks->motion(
            callbacks->data,
            object,
            event_time,
            event_id,
            event_x,
            event_y);
    }
}

static void swl_touch_handle_frame(
    void *listener_data,
    struct wl_touch *object)
{
    const struct swl_touch_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->frame) {
        callbacks->frame(
            callbacks->data,
            object);
    }
}

static void swl_touch_handle_cancel(
    void *listener_data,
    struct wl_touch *object)
{
    const struct swl_touch_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->cancel) {
        callbacks->cancel(
            callbacks->data,
            object);
    }
}

static void swl_touch_handle_shape(
    void *listener_data,
    struct wl_touch *object,
    int32_t event_id,
    wl_fixed_t event_major,
    wl_fixed_t event_minor)
{
    const struct swl_touch_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->shape) {
        callbacks->shape(
            callbacks->data,
            object,
            event_id,
            event_major,
            event_minor);
    }
}

static void swl_touch_handle_orientation(
    void *listener_data,
    struct wl_touch *object,
    int32_t event_id,
    wl_fixed_t event_orientation)
{
    const struct swl_touch_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->orientation) {
        callbacks->orientation(
            callbacks->data,
            object,
            event_id,
            event_orientation);
    }
}

static const struct wl_touch_listener
    swl_touch_listener = {
        .down = swl_touch_handle_down,
        .up = swl_touch_handle_up,
        .motion = swl_touch_handle_motion,
        .frame = swl_touch_handle_frame,
        .cancel = swl_touch_handle_cancel,
        .shape = swl_touch_handle_shape,
        .orientation = swl_touch_handle_orientation,
    };

int swl_touch_add_listener(
    struct wl_touch *object,
    const struct swl_touch_listener_callbacks *callbacks)
{
    return wl_touch_add_listener(
        object,
        &swl_touch_listener,
        (void *)callbacks);
}

static void swl_wp_fractional_scale_v1_handle_preferred_scale(
    void *listener_data,
    struct wp_fractional_scale_v1 *object,
    uint32_t event_scale)
{
    const struct swl_wp_fractional_scale_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->preferred_scale) {
        callbacks->preferred_scale(
            callbacks->data,
            object,
            event_scale);
    }
}

static const struct wp_fractional_scale_v1_listener
    swl_wp_fractional_scale_v1_listener = {
        .preferred_scale = swl_wp_fractional_scale_v1_handle_preferred_scale,
    };

int swl_wp_fractional_scale_v1_add_listener(
    struct wp_fractional_scale_v1 *object,
    const struct swl_wp_fractional_scale_v1_listener_callbacks *callbacks)
{
    return wp_fractional_scale_v1_add_listener(
        object,
        &swl_wp_fractional_scale_v1_listener,
        (void *)callbacks);
}

static void swl_xdg_activation_token_v1_handle_done(
    void *listener_data,
    struct xdg_activation_token_v1 *object,
    const char *event_token)
{
    const struct swl_xdg_activation_token_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->done) {
        callbacks->done(
            callbacks->data,
            object,
            event_token);
    }
}

static const struct xdg_activation_token_v1_listener
    swl_xdg_activation_token_v1_listener = {
        .done = swl_xdg_activation_token_v1_handle_done,
    };

int swl_xdg_activation_token_v1_add_listener(
    struct xdg_activation_token_v1 *object,
    const struct swl_xdg_activation_token_v1_listener_callbacks *callbacks)
{
    return xdg_activation_token_v1_add_listener(
        object,
        &swl_xdg_activation_token_v1_listener,
        (void *)callbacks);
}

static void swl_xdg_popup_handle_configure(
    void *listener_data,
    struct xdg_popup *object,
    int32_t event_x,
    int32_t event_y,
    int32_t event_width,
    int32_t event_height)
{
    const struct swl_xdg_popup_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->configure) {
        callbacks->configure(
            callbacks->data,
            object,
            event_x,
            event_y,
            event_width,
            event_height);
    }
}

static void swl_xdg_popup_handle_popup_done(
    void *listener_data,
    struct xdg_popup *object)
{
    const struct swl_xdg_popup_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->popup_done) {
        callbacks->popup_done(
            callbacks->data,
            object);
    }
}

static void swl_xdg_popup_handle_repositioned(
    void *listener_data,
    struct xdg_popup *object,
    uint32_t event_token)
{
    const struct swl_xdg_popup_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->repositioned) {
        callbacks->repositioned(
            callbacks->data,
            object,
            event_token);
    }
}

static const struct xdg_popup_listener
    swl_xdg_popup_listener = {
        .configure = swl_xdg_popup_handle_configure,
        .popup_done = swl_xdg_popup_handle_popup_done,
        .repositioned = swl_xdg_popup_handle_repositioned,
    };

int swl_xdg_popup_add_listener(
    struct xdg_popup *object,
    const struct swl_xdg_popup_listener_callbacks *callbacks)
{
    return xdg_popup_add_listener(
        object,
        &swl_xdg_popup_listener,
        (void *)callbacks);
}

static void swl_xdg_session_v1_handle_created(
    void *listener_data,
    struct xdg_session_v1 *object,
    const char *event_session_id)
{
    const struct swl_xdg_session_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->created) {
        callbacks->created(
            callbacks->data,
            object,
            event_session_id);
    }
}

static void swl_xdg_session_v1_handle_restored(
    void *listener_data,
    struct xdg_session_v1 *object)
{
    const struct swl_xdg_session_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->restored) {
        callbacks->restored(
            callbacks->data,
            object);
    }
}

static void swl_xdg_session_v1_handle_replaced(
    void *listener_data,
    struct xdg_session_v1 *object)
{
    const struct swl_xdg_session_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->replaced) {
        callbacks->replaced(
            callbacks->data,
            object);
    }
}

static const struct xdg_session_v1_listener
    swl_xdg_session_v1_listener = {
        .created = swl_xdg_session_v1_handle_created,
        .restored = swl_xdg_session_v1_handle_restored,
        .replaced = swl_xdg_session_v1_handle_replaced,
    };

int swl_xdg_session_v1_add_listener(
    struct xdg_session_v1 *object,
    const struct swl_xdg_session_v1_listener_callbacks *callbacks)
{
    return xdg_session_v1_add_listener(
        object,
        &swl_xdg_session_v1_listener,
        (void *)callbacks);
}

static void swl_xdg_surface_handle_configure(
    void *listener_data,
    struct xdg_surface *object,
    uint32_t event_serial)
{
    const struct swl_xdg_surface_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->configure) {
        callbacks->configure(
            callbacks->data,
            object,
            event_serial);
    }
}

static const struct xdg_surface_listener
    swl_xdg_surface_listener = {
        .configure = swl_xdg_surface_handle_configure,
    };

int swl_xdg_surface_add_listener(
    struct xdg_surface *object,
    const struct swl_xdg_surface_listener_callbacks *callbacks)
{
    return xdg_surface_add_listener(
        object,
        &swl_xdg_surface_listener,
        (void *)callbacks);
}

static void swl_xdg_toplevel_handle_configure(
    void *listener_data,
    struct xdg_toplevel *object,
    int32_t event_width,
    int32_t event_height,
    struct wl_array *event_states)
{
    const struct swl_xdg_toplevel_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->configure) {
        callbacks->configure(
            callbacks->data,
            object,
            event_width,
            event_height,
            event_states);
    }
}

static void swl_xdg_toplevel_handle_close(
    void *listener_data,
    struct xdg_toplevel *object)
{
    const struct swl_xdg_toplevel_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->close) {
        callbacks->close(
            callbacks->data,
            object);
    }
}

static void swl_xdg_toplevel_handle_configure_bounds(
    void *listener_data,
    struct xdg_toplevel *object,
    int32_t event_width,
    int32_t event_height)
{
    const struct swl_xdg_toplevel_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->configure_bounds) {
        callbacks->configure_bounds(
            callbacks->data,
            object,
            event_width,
            event_height);
    }
}

static void swl_xdg_toplevel_handle_wm_capabilities(
    void *listener_data,
    struct xdg_toplevel *object,
    struct wl_array *event_capabilities)
{
    const struct swl_xdg_toplevel_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->wm_capabilities) {
        callbacks->wm_capabilities(
            callbacks->data,
            object,
            event_capabilities);
    }
}

static const struct xdg_toplevel_listener
    swl_xdg_toplevel_listener = {
        .configure = swl_xdg_toplevel_handle_configure,
        .close = swl_xdg_toplevel_handle_close,
        .configure_bounds = swl_xdg_toplevel_handle_configure_bounds,
        .wm_capabilities = swl_xdg_toplevel_handle_wm_capabilities,
    };

int swl_xdg_toplevel_add_listener(
    struct xdg_toplevel *object,
    const struct swl_xdg_toplevel_listener_callbacks *callbacks)
{
    return xdg_toplevel_add_listener(
        object,
        &swl_xdg_toplevel_listener,
        (void *)callbacks);
}

static void swl_xdg_toplevel_icon_manager_v1_handle_icon_size(
    void *listener_data,
    struct xdg_toplevel_icon_manager_v1 *object,
    int32_t event_size)
{
    const struct swl_xdg_toplevel_icon_manager_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->icon_size) {
        callbacks->icon_size(
            callbacks->data,
            object,
            event_size);
    }
}

static void swl_xdg_toplevel_icon_manager_v1_handle_done(
    void *listener_data,
    struct xdg_toplevel_icon_manager_v1 *object)
{
    const struct swl_xdg_toplevel_icon_manager_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->done) {
        callbacks->done(
            callbacks->data,
            object);
    }
}

static const struct xdg_toplevel_icon_manager_v1_listener
    swl_xdg_toplevel_icon_manager_v1_listener = {
        .icon_size = swl_xdg_toplevel_icon_manager_v1_handle_icon_size,
        .done = swl_xdg_toplevel_icon_manager_v1_handle_done,
    };

int swl_xdg_toplevel_icon_manager_v1_add_listener(
    struct xdg_toplevel_icon_manager_v1 *object,
    const struct swl_xdg_toplevel_icon_manager_v1_listener_callbacks *callbacks)
{
    return xdg_toplevel_icon_manager_v1_add_listener(
        object,
        &swl_xdg_toplevel_icon_manager_v1_listener,
        (void *)callbacks);
}

static void swl_xdg_toplevel_session_v1_handle_restored(
    void *listener_data,
    struct xdg_toplevel_session_v1 *object)
{
    const struct swl_xdg_toplevel_session_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->restored) {
        callbacks->restored(
            callbacks->data,
            object);
    }
}

static const struct xdg_toplevel_session_v1_listener
    swl_xdg_toplevel_session_v1_listener = {
        .restored = swl_xdg_toplevel_session_v1_handle_restored,
    };

int swl_xdg_toplevel_session_v1_add_listener(
    struct xdg_toplevel_session_v1 *object,
    const struct swl_xdg_toplevel_session_v1_listener_callbacks *callbacks)
{
    return xdg_toplevel_session_v1_add_listener(
        object,
        &swl_xdg_toplevel_session_v1_listener,
        (void *)callbacks);
}

static void swl_xdg_wm_base_handle_ping(
    void *listener_data,
    struct xdg_wm_base *object,
    uint32_t event_serial)
{
    const struct swl_xdg_wm_base_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->ping) {
        callbacks->ping(
            callbacks->data,
            object,
            event_serial);
    }
}

static const struct xdg_wm_base_listener
    swl_xdg_wm_base_listener = {
        .ping = swl_xdg_wm_base_handle_ping,
    };

int swl_xdg_wm_base_add_listener(
    struct xdg_wm_base *object,
    const struct swl_xdg_wm_base_listener_callbacks *callbacks)
{
    return xdg_wm_base_add_listener(
        object,
        &swl_xdg_wm_base_listener,
        (void *)callbacks);
}

static void swl_zwlr_output_configuration_v1_handle_succeeded(
    void *listener_data,
    struct zwlr_output_configuration_v1 *object)
{
    const struct swl_zwlr_output_configuration_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->succeeded) {
        callbacks->succeeded(
            callbacks->data,
            object);
    }
}

static void swl_zwlr_output_configuration_v1_handle_failed(
    void *listener_data,
    struct zwlr_output_configuration_v1 *object)
{
    const struct swl_zwlr_output_configuration_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->failed) {
        callbacks->failed(
            callbacks->data,
            object);
    }
}

static void swl_zwlr_output_configuration_v1_handle_cancelled(
    void *listener_data,
    struct zwlr_output_configuration_v1 *object)
{
    const struct swl_zwlr_output_configuration_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->cancelled) {
        callbacks->cancelled(
            callbacks->data,
            object);
    }
}

static const struct zwlr_output_configuration_v1_listener
    swl_zwlr_output_configuration_v1_listener = {
        .succeeded = swl_zwlr_output_configuration_v1_handle_succeeded,
        .failed = swl_zwlr_output_configuration_v1_handle_failed,
        .cancelled = swl_zwlr_output_configuration_v1_handle_cancelled,
    };

int swl_zwlr_output_configuration_v1_add_listener(
    struct zwlr_output_configuration_v1 *object,
    const struct swl_zwlr_output_configuration_v1_listener_callbacks *callbacks)
{
    return zwlr_output_configuration_v1_add_listener(
        object,
        &swl_zwlr_output_configuration_v1_listener,
        (void *)callbacks);
}

static void swl_zwlr_output_head_v1_handle_name(
    void *listener_data,
    struct zwlr_output_head_v1 *object,
    const char *event_name)
{
    const struct swl_zwlr_output_head_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->name) {
        callbacks->name(
            callbacks->data,
            object,
            event_name);
    }
}

static void swl_zwlr_output_head_v1_handle_description(
    void *listener_data,
    struct zwlr_output_head_v1 *object,
    const char *event_description)
{
    const struct swl_zwlr_output_head_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->description) {
        callbacks->description(
            callbacks->data,
            object,
            event_description);
    }
}

static void swl_zwlr_output_head_v1_handle_physical_size(
    void *listener_data,
    struct zwlr_output_head_v1 *object,
    int32_t event_width,
    int32_t event_height)
{
    const struct swl_zwlr_output_head_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->physical_size) {
        callbacks->physical_size(
            callbacks->data,
            object,
            event_width,
            event_height);
    }
}

static void swl_zwlr_output_head_v1_handle_mode(
    void *listener_data,
    struct zwlr_output_head_v1 *object,
    struct zwlr_output_mode_v1 *event_mode)
{
    const struct swl_zwlr_output_head_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->mode) {
        callbacks->mode(
            callbacks->data,
            object,
            event_mode);
    }
}

static void swl_zwlr_output_head_v1_handle_enabled(
    void *listener_data,
    struct zwlr_output_head_v1 *object,
    int32_t event_enabled)
{
    const struct swl_zwlr_output_head_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->enabled) {
        callbacks->enabled(
            callbacks->data,
            object,
            event_enabled);
    }
}

static void swl_zwlr_output_head_v1_handle_current_mode(
    void *listener_data,
    struct zwlr_output_head_v1 *object,
    struct zwlr_output_mode_v1 *event_mode)
{
    const struct swl_zwlr_output_head_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->current_mode) {
        callbacks->current_mode(
            callbacks->data,
            object,
            event_mode);
    }
}

static void swl_zwlr_output_head_v1_handle_position(
    void *listener_data,
    struct zwlr_output_head_v1 *object,
    int32_t event_x,
    int32_t event_y)
{
    const struct swl_zwlr_output_head_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->position) {
        callbacks->position(
            callbacks->data,
            object,
            event_x,
            event_y);
    }
}

static void swl_zwlr_output_head_v1_handle_transform(
    void *listener_data,
    struct zwlr_output_head_v1 *object,
    int32_t event_transform)
{
    const struct swl_zwlr_output_head_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->transform) {
        callbacks->transform(
            callbacks->data,
            object,
            event_transform);
    }
}

static void swl_zwlr_output_head_v1_handle_scale(
    void *listener_data,
    struct zwlr_output_head_v1 *object,
    wl_fixed_t event_scale)
{
    const struct swl_zwlr_output_head_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->scale) {
        callbacks->scale(
            callbacks->data,
            object,
            event_scale);
    }
}

static void swl_zwlr_output_head_v1_handle_finished(
    void *listener_data,
    struct zwlr_output_head_v1 *object)
{
    const struct swl_zwlr_output_head_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->finished) {
        callbacks->finished(
            callbacks->data,
            object);
    }
}

static void swl_zwlr_output_head_v1_handle_make(
    void *listener_data,
    struct zwlr_output_head_v1 *object,
    const char *event_make)
{
    const struct swl_zwlr_output_head_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->make) {
        callbacks->make(
            callbacks->data,
            object,
            event_make);
    }
}

static void swl_zwlr_output_head_v1_handle_model(
    void *listener_data,
    struct zwlr_output_head_v1 *object,
    const char *event_model)
{
    const struct swl_zwlr_output_head_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->model) {
        callbacks->model(
            callbacks->data,
            object,
            event_model);
    }
}

static void swl_zwlr_output_head_v1_handle_serial_number(
    void *listener_data,
    struct zwlr_output_head_v1 *object,
    const char *event_serial_number)
{
    const struct swl_zwlr_output_head_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->serial_number) {
        callbacks->serial_number(
            callbacks->data,
            object,
            event_serial_number);
    }
}

static void swl_zwlr_output_head_v1_handle_adaptive_sync(
    void *listener_data,
    struct zwlr_output_head_v1 *object,
    uint32_t event_state)
{
    const struct swl_zwlr_output_head_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->adaptive_sync) {
        callbacks->adaptive_sync(
            callbacks->data,
            object,
            event_state);
    }
}

static const struct zwlr_output_head_v1_listener
    swl_zwlr_output_head_v1_listener = {
        .name = swl_zwlr_output_head_v1_handle_name,
        .description = swl_zwlr_output_head_v1_handle_description,
        .physical_size = swl_zwlr_output_head_v1_handle_physical_size,
        .mode = swl_zwlr_output_head_v1_handle_mode,
        .enabled = swl_zwlr_output_head_v1_handle_enabled,
        .current_mode = swl_zwlr_output_head_v1_handle_current_mode,
        .position = swl_zwlr_output_head_v1_handle_position,
        .transform = swl_zwlr_output_head_v1_handle_transform,
        .scale = swl_zwlr_output_head_v1_handle_scale,
        .finished = swl_zwlr_output_head_v1_handle_finished,
        .make = swl_zwlr_output_head_v1_handle_make,
        .model = swl_zwlr_output_head_v1_handle_model,
        .serial_number = swl_zwlr_output_head_v1_handle_serial_number,
        .adaptive_sync = swl_zwlr_output_head_v1_handle_adaptive_sync,
    };

int swl_zwlr_output_head_v1_add_listener(
    struct zwlr_output_head_v1 *object,
    const struct swl_zwlr_output_head_v1_listener_callbacks *callbacks)
{
    return zwlr_output_head_v1_add_listener(
        object,
        &swl_zwlr_output_head_v1_listener,
        (void *)callbacks);
}

static void swl_zwlr_output_manager_v1_handle_head(
    void *listener_data,
    struct zwlr_output_manager_v1 *object,
    struct zwlr_output_head_v1 *event_head)
{
    const struct swl_zwlr_output_manager_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->head) {
        callbacks->head(
            callbacks->data,
            object,
            event_head);
    }
}

static void swl_zwlr_output_manager_v1_handle_done(
    void *listener_data,
    struct zwlr_output_manager_v1 *object,
    uint32_t event_serial)
{
    const struct swl_zwlr_output_manager_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->done) {
        callbacks->done(
            callbacks->data,
            object,
            event_serial);
    }
}

static void swl_zwlr_output_manager_v1_handle_finished(
    void *listener_data,
    struct zwlr_output_manager_v1 *object)
{
    const struct swl_zwlr_output_manager_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->finished) {
        callbacks->finished(
            callbacks->data,
            object);
    }
}

static const struct zwlr_output_manager_v1_listener
    swl_zwlr_output_manager_v1_listener = {
        .head = swl_zwlr_output_manager_v1_handle_head,
        .done = swl_zwlr_output_manager_v1_handle_done,
        .finished = swl_zwlr_output_manager_v1_handle_finished,
    };

int swl_zwlr_output_manager_v1_add_listener(
    struct zwlr_output_manager_v1 *object,
    const struct swl_zwlr_output_manager_v1_listener_callbacks *callbacks)
{
    return zwlr_output_manager_v1_add_listener(
        object,
        &swl_zwlr_output_manager_v1_listener,
        (void *)callbacks);
}

static void swl_zwlr_output_mode_v1_handle_size(
    void *listener_data,
    struct zwlr_output_mode_v1 *object,
    int32_t event_width,
    int32_t event_height)
{
    const struct swl_zwlr_output_mode_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->size) {
        callbacks->size(
            callbacks->data,
            object,
            event_width,
            event_height);
    }
}

static void swl_zwlr_output_mode_v1_handle_refresh(
    void *listener_data,
    struct zwlr_output_mode_v1 *object,
    int32_t event_refresh)
{
    const struct swl_zwlr_output_mode_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->refresh) {
        callbacks->refresh(
            callbacks->data,
            object,
            event_refresh);
    }
}

static void swl_zwlr_output_mode_v1_handle_preferred(
    void *listener_data,
    struct zwlr_output_mode_v1 *object)
{
    const struct swl_zwlr_output_mode_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->preferred) {
        callbacks->preferred(
            callbacks->data,
            object);
    }
}

static void swl_zwlr_output_mode_v1_handle_finished(
    void *listener_data,
    struct zwlr_output_mode_v1 *object)
{
    const struct swl_zwlr_output_mode_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->finished) {
        callbacks->finished(
            callbacks->data,
            object);
    }
}

static const struct zwlr_output_mode_v1_listener
    swl_zwlr_output_mode_v1_listener = {
        .size = swl_zwlr_output_mode_v1_handle_size,
        .refresh = swl_zwlr_output_mode_v1_handle_refresh,
        .preferred = swl_zwlr_output_mode_v1_handle_preferred,
        .finished = swl_zwlr_output_mode_v1_handle_finished,
    };

int swl_zwlr_output_mode_v1_add_listener(
    struct zwlr_output_mode_v1 *object,
    const struct swl_zwlr_output_mode_v1_listener_callbacks *callbacks)
{
    return zwlr_output_mode_v1_add_listener(
        object,
        &swl_zwlr_output_mode_v1_listener,
        (void *)callbacks);
}

static void swl_zwp_confined_pointer_v1_handle_confined(
    void *listener_data,
    struct zwp_confined_pointer_v1 *object)
{
    const struct swl_zwp_confined_pointer_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->confined) {
        callbacks->confined(
            callbacks->data,
            object);
    }
}

static void swl_zwp_confined_pointer_v1_handle_unconfined(
    void *listener_data,
    struct zwp_confined_pointer_v1 *object)
{
    const struct swl_zwp_confined_pointer_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->unconfined) {
        callbacks->unconfined(
            callbacks->data,
            object);
    }
}

static const struct zwp_confined_pointer_v1_listener
    swl_zwp_confined_pointer_v1_listener = {
        .confined = swl_zwp_confined_pointer_v1_handle_confined,
        .unconfined = swl_zwp_confined_pointer_v1_handle_unconfined,
    };

int swl_zwp_confined_pointer_v1_add_listener(
    struct zwp_confined_pointer_v1 *object,
    const struct swl_zwp_confined_pointer_v1_listener_callbacks *callbacks)
{
    return zwp_confined_pointer_v1_add_listener(
        object,
        &swl_zwp_confined_pointer_v1_listener,
        (void *)callbacks);
}

static void swl_zwp_linux_buffer_params_handle_created(
    void *listener_data,
    struct zwp_linux_buffer_params_v1 *object,
    struct wl_buffer *event_buffer)
{
    const struct swl_zwp_linux_buffer_params_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->created) {
        callbacks->created(
            callbacks->data,
            object,
            event_buffer);
    }
}

static void swl_zwp_linux_buffer_params_handle_failed(
    void *listener_data,
    struct zwp_linux_buffer_params_v1 *object)
{
    const struct swl_zwp_linux_buffer_params_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->failed) {
        callbacks->failed(
            callbacks->data,
            object);
    }
}

static const struct zwp_linux_buffer_params_v1_listener
    swl_zwp_linux_buffer_params_listener = {
        .created = swl_zwp_linux_buffer_params_handle_created,
        .failed = swl_zwp_linux_buffer_params_handle_failed,
    };

int swl_zwp_linux_buffer_params_v1_add_listener(
    struct zwp_linux_buffer_params_v1 *object,
    const struct swl_zwp_linux_buffer_params_listener_callbacks *callbacks)
{
    return zwp_linux_buffer_params_v1_add_listener(
        object,
        &swl_zwp_linux_buffer_params_listener,
        (void *)callbacks);
}

static void swl_zwp_linux_dmabuf_feedback_handle_done(
    void *listener_data,
    struct zwp_linux_dmabuf_feedback_v1 *object)
{
    const struct swl_zwp_linux_dmabuf_feedback_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->done) {
        callbacks->done(
            callbacks->data,
            object);
    }
}

static void swl_zwp_linux_dmabuf_feedback_handle_format_table(
    void *listener_data,
    struct zwp_linux_dmabuf_feedback_v1 *object,
    int32_t event_fd,
    uint32_t event_size)
{
    const struct swl_zwp_linux_dmabuf_feedback_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->format_table) {
        callbacks->format_table(
            callbacks->data,
            object,
            event_fd,
            event_size);
    }
}

static void swl_zwp_linux_dmabuf_feedback_handle_main_device(
    void *listener_data,
    struct zwp_linux_dmabuf_feedback_v1 *object,
    struct wl_array *event_device)
{
    const struct swl_zwp_linux_dmabuf_feedback_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->main_device) {
        callbacks->main_device(
            callbacks->data,
            object,
            event_device);
    }
}

static void swl_zwp_linux_dmabuf_feedback_handle_tranche_done(
    void *listener_data,
    struct zwp_linux_dmabuf_feedback_v1 *object)
{
    const struct swl_zwp_linux_dmabuf_feedback_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->tranche_done) {
        callbacks->tranche_done(
            callbacks->data,
            object);
    }
}

static void swl_zwp_linux_dmabuf_feedback_handle_tranche_target_device(
    void *listener_data,
    struct zwp_linux_dmabuf_feedback_v1 *object,
    struct wl_array *event_device)
{
    const struct swl_zwp_linux_dmabuf_feedback_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->tranche_target_device) {
        callbacks->tranche_target_device(
            callbacks->data,
            object,
            event_device);
    }
}

static void swl_zwp_linux_dmabuf_feedback_handle_tranche_formats(
    void *listener_data,
    struct zwp_linux_dmabuf_feedback_v1 *object,
    struct wl_array *event_indices)
{
    const struct swl_zwp_linux_dmabuf_feedback_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->tranche_formats) {
        callbacks->tranche_formats(
            callbacks->data,
            object,
            event_indices);
    }
}

static void swl_zwp_linux_dmabuf_feedback_handle_tranche_flags(
    void *listener_data,
    struct zwp_linux_dmabuf_feedback_v1 *object,
    uint32_t event_flags)
{
    const struct swl_zwp_linux_dmabuf_feedback_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->tranche_flags) {
        callbacks->tranche_flags(
            callbacks->data,
            object,
            event_flags);
    }
}

static const struct zwp_linux_dmabuf_feedback_v1_listener
    swl_zwp_linux_dmabuf_feedback_listener = {
        .done = swl_zwp_linux_dmabuf_feedback_handle_done,
        .format_table = swl_zwp_linux_dmabuf_feedback_handle_format_table,
        .main_device = swl_zwp_linux_dmabuf_feedback_handle_main_device,
        .tranche_done = swl_zwp_linux_dmabuf_feedback_handle_tranche_done,
        .tranche_target_device = swl_zwp_linux_dmabuf_feedback_handle_tranche_target_device,
        .tranche_formats = swl_zwp_linux_dmabuf_feedback_handle_tranche_formats,
        .tranche_flags = swl_zwp_linux_dmabuf_feedback_handle_tranche_flags,
    };

int swl_zwp_linux_dmabuf_feedback_v1_add_listener(
    struct zwp_linux_dmabuf_feedback_v1 *object,
    const struct swl_zwp_linux_dmabuf_feedback_listener_callbacks *callbacks)
{
    return zwp_linux_dmabuf_feedback_v1_add_listener(
        object,
        &swl_zwp_linux_dmabuf_feedback_listener,
        (void *)callbacks);
}

static void swl_zwp_locked_pointer_v1_handle_locked(
    void *listener_data,
    struct zwp_locked_pointer_v1 *object)
{
    const struct swl_zwp_locked_pointer_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->locked) {
        callbacks->locked(
            callbacks->data,
            object);
    }
}

static void swl_zwp_locked_pointer_v1_handle_unlocked(
    void *listener_data,
    struct zwp_locked_pointer_v1 *object)
{
    const struct swl_zwp_locked_pointer_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->unlocked) {
        callbacks->unlocked(
            callbacks->data,
            object);
    }
}

static const struct zwp_locked_pointer_v1_listener
    swl_zwp_locked_pointer_v1_listener = {
        .locked = swl_zwp_locked_pointer_v1_handle_locked,
        .unlocked = swl_zwp_locked_pointer_v1_handle_unlocked,
    };

int swl_zwp_locked_pointer_v1_add_listener(
    struct zwp_locked_pointer_v1 *object,
    const struct swl_zwp_locked_pointer_v1_listener_callbacks *callbacks)
{
    return zwp_locked_pointer_v1_add_listener(
        object,
        &swl_zwp_locked_pointer_v1_listener,
        (void *)callbacks);
}

static void swl_zwp_pointer_gesture_hold_v1_handle_begin(
    void *listener_data,
    struct zwp_pointer_gesture_hold_v1 *object,
    uint32_t event_serial,
    uint32_t event_time,
    struct wl_surface *event_surface,
    uint32_t event_fingers)
{
    const struct swl_zwp_pointer_gesture_hold_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->begin) {
        callbacks->begin(
            callbacks->data,
            object,
            event_serial,
            event_time,
            event_surface,
            event_fingers);
    }
}

static void swl_zwp_pointer_gesture_hold_v1_handle_end(
    void *listener_data,
    struct zwp_pointer_gesture_hold_v1 *object,
    uint32_t event_serial,
    uint32_t event_time,
    int32_t event_cancelled)
{
    const struct swl_zwp_pointer_gesture_hold_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->end) {
        callbacks->end(
            callbacks->data,
            object,
            event_serial,
            event_time,
            event_cancelled);
    }
}

static const struct zwp_pointer_gesture_hold_v1_listener
    swl_zwp_pointer_gesture_hold_v1_listener = {
        .begin = swl_zwp_pointer_gesture_hold_v1_handle_begin,
        .end = swl_zwp_pointer_gesture_hold_v1_handle_end,
    };

int swl_zwp_pointer_gesture_hold_v1_add_listener(
    struct zwp_pointer_gesture_hold_v1 *object,
    const struct swl_zwp_pointer_gesture_hold_v1_listener_callbacks *callbacks)
{
    return zwp_pointer_gesture_hold_v1_add_listener(
        object,
        &swl_zwp_pointer_gesture_hold_v1_listener,
        (void *)callbacks);
}

static void swl_zwp_pointer_gesture_pinch_v1_handle_begin(
    void *listener_data,
    struct zwp_pointer_gesture_pinch_v1 *object,
    uint32_t event_serial,
    uint32_t event_time,
    struct wl_surface *event_surface,
    uint32_t event_fingers)
{
    const struct swl_zwp_pointer_gesture_pinch_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->begin) {
        callbacks->begin(
            callbacks->data,
            object,
            event_serial,
            event_time,
            event_surface,
            event_fingers);
    }
}

static void swl_zwp_pointer_gesture_pinch_v1_handle_update(
    void *listener_data,
    struct zwp_pointer_gesture_pinch_v1 *object,
    uint32_t event_time,
    wl_fixed_t event_dx,
    wl_fixed_t event_dy,
    wl_fixed_t event_scale,
    wl_fixed_t event_rotation)
{
    const struct swl_zwp_pointer_gesture_pinch_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->update) {
        callbacks->update(
            callbacks->data,
            object,
            event_time,
            event_dx,
            event_dy,
            event_scale,
            event_rotation);
    }
}

static void swl_zwp_pointer_gesture_pinch_v1_handle_end(
    void *listener_data,
    struct zwp_pointer_gesture_pinch_v1 *object,
    uint32_t event_serial,
    uint32_t event_time,
    int32_t event_cancelled)
{
    const struct swl_zwp_pointer_gesture_pinch_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->end) {
        callbacks->end(
            callbacks->data,
            object,
            event_serial,
            event_time,
            event_cancelled);
    }
}

static const struct zwp_pointer_gesture_pinch_v1_listener
    swl_zwp_pointer_gesture_pinch_v1_listener = {
        .begin = swl_zwp_pointer_gesture_pinch_v1_handle_begin,
        .update = swl_zwp_pointer_gesture_pinch_v1_handle_update,
        .end = swl_zwp_pointer_gesture_pinch_v1_handle_end,
    };

int swl_zwp_pointer_gesture_pinch_v1_add_listener(
    struct zwp_pointer_gesture_pinch_v1 *object,
    const struct swl_zwp_pointer_gesture_pinch_v1_listener_callbacks *callbacks)
{
    return zwp_pointer_gesture_pinch_v1_add_listener(
        object,
        &swl_zwp_pointer_gesture_pinch_v1_listener,
        (void *)callbacks);
}

static void swl_zwp_pointer_gesture_swipe_v1_handle_begin(
    void *listener_data,
    struct zwp_pointer_gesture_swipe_v1 *object,
    uint32_t event_serial,
    uint32_t event_time,
    struct wl_surface *event_surface,
    uint32_t event_fingers)
{
    const struct swl_zwp_pointer_gesture_swipe_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->begin) {
        callbacks->begin(
            callbacks->data,
            object,
            event_serial,
            event_time,
            event_surface,
            event_fingers);
    }
}

static void swl_zwp_pointer_gesture_swipe_v1_handle_update(
    void *listener_data,
    struct zwp_pointer_gesture_swipe_v1 *object,
    uint32_t event_time,
    wl_fixed_t event_dx,
    wl_fixed_t event_dy)
{
    const struct swl_zwp_pointer_gesture_swipe_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->update) {
        callbacks->update(
            callbacks->data,
            object,
            event_time,
            event_dx,
            event_dy);
    }
}

static void swl_zwp_pointer_gesture_swipe_v1_handle_end(
    void *listener_data,
    struct zwp_pointer_gesture_swipe_v1 *object,
    uint32_t event_serial,
    uint32_t event_time,
    int32_t event_cancelled)
{
    const struct swl_zwp_pointer_gesture_swipe_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->end) {
        callbacks->end(
            callbacks->data,
            object,
            event_serial,
            event_time,
            event_cancelled);
    }
}

static const struct zwp_pointer_gesture_swipe_v1_listener
    swl_zwp_pointer_gesture_swipe_v1_listener = {
        .begin = swl_zwp_pointer_gesture_swipe_v1_handle_begin,
        .update = swl_zwp_pointer_gesture_swipe_v1_handle_update,
        .end = swl_zwp_pointer_gesture_swipe_v1_handle_end,
    };

int swl_zwp_pointer_gesture_swipe_v1_add_listener(
    struct zwp_pointer_gesture_swipe_v1 *object,
    const struct swl_zwp_pointer_gesture_swipe_v1_listener_callbacks *callbacks)
{
    return zwp_pointer_gesture_swipe_v1_add_listener(
        object,
        &swl_zwp_pointer_gesture_swipe_v1_listener,
        (void *)callbacks);
}

static void swl_primary_selection_device_handle_data_offer(
    void *listener_data,
    struct zwp_primary_selection_device_v1 *object,
    struct zwp_primary_selection_offer_v1 *event_offer)
{
    const struct swl_primary_selection_device_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->data_offer) {
        callbacks->data_offer(
            callbacks->data,
            object,
            event_offer);
    }
}

static void swl_primary_selection_device_handle_selection(
    void *listener_data,
    struct zwp_primary_selection_device_v1 *object,
    struct zwp_primary_selection_offer_v1 *event_id)
{
    const struct swl_primary_selection_device_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->selection) {
        callbacks->selection(
            callbacks->data,
            object,
            event_id);
    }
}

static const struct zwp_primary_selection_device_v1_listener
    swl_primary_selection_device_listener = {
        .data_offer = swl_primary_selection_device_handle_data_offer,
        .selection = swl_primary_selection_device_handle_selection,
    };

int swl_primary_selection_device_add_listener(
    struct zwp_primary_selection_device_v1 *object,
    const struct swl_primary_selection_device_listener_callbacks *callbacks)
{
    return zwp_primary_selection_device_v1_add_listener(
        object,
        &swl_primary_selection_device_listener,
        (void *)callbacks);
}

static void swl_primary_selection_offer_handle_offer(
    void *listener_data,
    struct zwp_primary_selection_offer_v1 *object,
    const char *event_mime_type)
{
    const struct swl_primary_selection_offer_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->offer) {
        callbacks->offer(
            callbacks->data,
            object,
            event_mime_type);
    }
}

static const struct zwp_primary_selection_offer_v1_listener
    swl_primary_selection_offer_listener = {
        .offer = swl_primary_selection_offer_handle_offer,
    };

int swl_primary_selection_offer_add_listener(
    struct zwp_primary_selection_offer_v1 *object,
    const struct swl_primary_selection_offer_listener_callbacks *callbacks)
{
    return zwp_primary_selection_offer_v1_add_listener(
        object,
        &swl_primary_selection_offer_listener,
        (void *)callbacks);
}

static void swl_primary_selection_source_handle_send(
    void *listener_data,
    struct zwp_primary_selection_source_v1 *object,
    const char *event_mime_type,
    int32_t event_fd)
{
    const struct swl_primary_selection_source_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->send) {
        callbacks->send(
            callbacks->data,
            object,
            event_mime_type,
            event_fd);
    }
}

static void swl_primary_selection_source_handle_cancelled(
    void *listener_data,
    struct zwp_primary_selection_source_v1 *object)
{
    const struct swl_primary_selection_source_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->cancelled) {
        callbacks->cancelled(
            callbacks->data,
            object);
    }
}

static const struct zwp_primary_selection_source_v1_listener
    swl_primary_selection_source_listener = {
        .send = swl_primary_selection_source_handle_send,
        .cancelled = swl_primary_selection_source_handle_cancelled,
    };

int swl_primary_selection_source_add_listener(
    struct zwp_primary_selection_source_v1 *object,
    const struct swl_primary_selection_source_listener_callbacks *callbacks)
{
    return zwp_primary_selection_source_v1_add_listener(
        object,
        &swl_primary_selection_source_listener,
        (void *)callbacks);
}

static void swl_zwp_relative_pointer_v1_handle_relative_motion(
    void *listener_data,
    struct zwp_relative_pointer_v1 *object,
    uint32_t event_utime_hi,
    uint32_t event_utime_lo,
    wl_fixed_t event_dx,
    wl_fixed_t event_dy,
    wl_fixed_t event_dx_unaccel,
    wl_fixed_t event_dy_unaccel)
{
    const struct swl_zwp_relative_pointer_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->relative_motion) {
        callbacks->relative_motion(
            callbacks->data,
            object,
            event_utime_hi,
            event_utime_lo,
            event_dx,
            event_dy,
            event_dx_unaccel,
            event_dy_unaccel);
    }
}

static const struct zwp_relative_pointer_v1_listener
    swl_zwp_relative_pointer_v1_listener = {
        .relative_motion = swl_zwp_relative_pointer_v1_handle_relative_motion,
    };

int swl_zwp_relative_pointer_v1_add_listener(
    struct zwp_relative_pointer_v1 *object,
    const struct swl_zwp_relative_pointer_v1_listener_callbacks *callbacks)
{
    return zwp_relative_pointer_v1_add_listener(
        object,
        &swl_zwp_relative_pointer_v1_listener,
        (void *)callbacks);
}

static void swl_zwp_tablet_pad_group_v2_handle_buttons(
    void *listener_data,
    struct zwp_tablet_pad_group_v2 *object,
    struct wl_array *event_buttons)
{
    const struct swl_zwp_tablet_pad_group_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->buttons) {
        callbacks->buttons(
            callbacks->data,
            object,
            event_buttons);
    }
}

static void swl_zwp_tablet_pad_group_v2_handle_ring(
    void *listener_data,
    struct zwp_tablet_pad_group_v2 *object,
    struct zwp_tablet_pad_ring_v2 *event_ring)
{
    const struct swl_zwp_tablet_pad_group_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->ring) {
        callbacks->ring(
            callbacks->data,
            object,
            event_ring);
    }
}

static void swl_zwp_tablet_pad_group_v2_handle_strip(
    void *listener_data,
    struct zwp_tablet_pad_group_v2 *object,
    struct zwp_tablet_pad_strip_v2 *event_strip)
{
    const struct swl_zwp_tablet_pad_group_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->strip) {
        callbacks->strip(
            callbacks->data,
            object,
            event_strip);
    }
}

static void swl_zwp_tablet_pad_group_v2_handle_modes(
    void *listener_data,
    struct zwp_tablet_pad_group_v2 *object,
    uint32_t event_modes)
{
    const struct swl_zwp_tablet_pad_group_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->modes) {
        callbacks->modes(
            callbacks->data,
            object,
            event_modes);
    }
}

static void swl_zwp_tablet_pad_group_v2_handle_done(
    void *listener_data,
    struct zwp_tablet_pad_group_v2 *object)
{
    const struct swl_zwp_tablet_pad_group_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->done) {
        callbacks->done(
            callbacks->data,
            object);
    }
}

static void swl_zwp_tablet_pad_group_v2_handle_mode_switch(
    void *listener_data,
    struct zwp_tablet_pad_group_v2 *object,
    uint32_t event_time,
    uint32_t event_serial,
    uint32_t event_mode)
{
    const struct swl_zwp_tablet_pad_group_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->mode_switch) {
        callbacks->mode_switch(
            callbacks->data,
            object,
            event_time,
            event_serial,
            event_mode);
    }
}

static void swl_zwp_tablet_pad_group_v2_handle_dial(
    void *listener_data,
    struct zwp_tablet_pad_group_v2 *object,
    struct zwp_tablet_pad_dial_v2 *event_dial)
{
    const struct swl_zwp_tablet_pad_group_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->dial) {
        callbacks->dial(
            callbacks->data,
            object,
            event_dial);
    }
}

static const struct zwp_tablet_pad_group_v2_listener
    swl_zwp_tablet_pad_group_v2_listener = {
        .buttons = swl_zwp_tablet_pad_group_v2_handle_buttons,
        .ring = swl_zwp_tablet_pad_group_v2_handle_ring,
        .strip = swl_zwp_tablet_pad_group_v2_handle_strip,
        .modes = swl_zwp_tablet_pad_group_v2_handle_modes,
        .done = swl_zwp_tablet_pad_group_v2_handle_done,
        .mode_switch = swl_zwp_tablet_pad_group_v2_handle_mode_switch,
        .dial = swl_zwp_tablet_pad_group_v2_handle_dial,
    };

int swl_zwp_tablet_pad_group_v2_add_listener(
    struct zwp_tablet_pad_group_v2 *object,
    const struct swl_zwp_tablet_pad_group_v2_listener_callbacks *callbacks)
{
    return zwp_tablet_pad_group_v2_add_listener(
        object,
        &swl_zwp_tablet_pad_group_v2_listener,
        (void *)callbacks);
}

static void swl_zwp_tablet_pad_v2_handle_group(
    void *listener_data,
    struct zwp_tablet_pad_v2 *object,
    struct zwp_tablet_pad_group_v2 *event_pad_group)
{
    const struct swl_zwp_tablet_pad_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->group) {
        callbacks->group(
            callbacks->data,
            object,
            event_pad_group);
    }
}

static void swl_zwp_tablet_pad_v2_handle_path(
    void *listener_data,
    struct zwp_tablet_pad_v2 *object,
    const char *event_path)
{
    const struct swl_zwp_tablet_pad_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->path) {
        callbacks->path(
            callbacks->data,
            object,
            event_path);
    }
}

static void swl_zwp_tablet_pad_v2_handle_buttons(
    void *listener_data,
    struct zwp_tablet_pad_v2 *object,
    uint32_t event_buttons)
{
    const struct swl_zwp_tablet_pad_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->buttons) {
        callbacks->buttons(
            callbacks->data,
            object,
            event_buttons);
    }
}

static void swl_zwp_tablet_pad_v2_handle_done(
    void *listener_data,
    struct zwp_tablet_pad_v2 *object)
{
    const struct swl_zwp_tablet_pad_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->done) {
        callbacks->done(
            callbacks->data,
            object);
    }
}

static void swl_zwp_tablet_pad_v2_handle_button(
    void *listener_data,
    struct zwp_tablet_pad_v2 *object,
    uint32_t event_time,
    uint32_t event_button,
    uint32_t event_state)
{
    const struct swl_zwp_tablet_pad_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->button) {
        callbacks->button(
            callbacks->data,
            object,
            event_time,
            event_button,
            event_state);
    }
}

static void swl_zwp_tablet_pad_v2_handle_enter(
    void *listener_data,
    struct zwp_tablet_pad_v2 *object,
    uint32_t event_serial,
    struct zwp_tablet_v2 *event_tablet,
    struct wl_surface *event_surface)
{
    const struct swl_zwp_tablet_pad_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->enter) {
        callbacks->enter(
            callbacks->data,
            object,
            event_serial,
            event_tablet,
            event_surface);
    }
}

static void swl_zwp_tablet_pad_v2_handle_leave(
    void *listener_data,
    struct zwp_tablet_pad_v2 *object,
    uint32_t event_serial,
    struct wl_surface *event_surface)
{
    const struct swl_zwp_tablet_pad_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->leave) {
        callbacks->leave(
            callbacks->data,
            object,
            event_serial,
            event_surface);
    }
}

static void swl_zwp_tablet_pad_v2_handle_removed(
    void *listener_data,
    struct zwp_tablet_pad_v2 *object)
{
    const struct swl_zwp_tablet_pad_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->removed) {
        callbacks->removed(
            callbacks->data,
            object);
    }
}

static const struct zwp_tablet_pad_v2_listener
    swl_zwp_tablet_pad_v2_listener = {
        .group = swl_zwp_tablet_pad_v2_handle_group,
        .path = swl_zwp_tablet_pad_v2_handle_path,
        .buttons = swl_zwp_tablet_pad_v2_handle_buttons,
        .done = swl_zwp_tablet_pad_v2_handle_done,
        .button = swl_zwp_tablet_pad_v2_handle_button,
        .enter = swl_zwp_tablet_pad_v2_handle_enter,
        .leave = swl_zwp_tablet_pad_v2_handle_leave,
        .removed = swl_zwp_tablet_pad_v2_handle_removed,
    };

int swl_zwp_tablet_pad_v2_add_listener(
    struct zwp_tablet_pad_v2 *object,
    const struct swl_zwp_tablet_pad_v2_listener_callbacks *callbacks)
{
    return zwp_tablet_pad_v2_add_listener(
        object,
        &swl_zwp_tablet_pad_v2_listener,
        (void *)callbacks);
}

static void swl_zwp_tablet_seat_v2_handle_tablet_added(
    void *listener_data,
    struct zwp_tablet_seat_v2 *object,
    struct zwp_tablet_v2 *event_id)
{
    const struct swl_zwp_tablet_seat_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->tablet_added) {
        callbacks->tablet_added(
            callbacks->data,
            object,
            event_id);
    }
}

static void swl_zwp_tablet_seat_v2_handle_tool_added(
    void *listener_data,
    struct zwp_tablet_seat_v2 *object,
    struct zwp_tablet_tool_v2 *event_id)
{
    const struct swl_zwp_tablet_seat_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->tool_added) {
        callbacks->tool_added(
            callbacks->data,
            object,
            event_id);
    }
}

static void swl_zwp_tablet_seat_v2_handle_pad_added(
    void *listener_data,
    struct zwp_tablet_seat_v2 *object,
    struct zwp_tablet_pad_v2 *event_id)
{
    const struct swl_zwp_tablet_seat_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->pad_added) {
        callbacks->pad_added(
            callbacks->data,
            object,
            event_id);
    }
}

static const struct zwp_tablet_seat_v2_listener
    swl_zwp_tablet_seat_v2_listener = {
        .tablet_added = swl_zwp_tablet_seat_v2_handle_tablet_added,
        .tool_added = swl_zwp_tablet_seat_v2_handle_tool_added,
        .pad_added = swl_zwp_tablet_seat_v2_handle_pad_added,
    };

int swl_zwp_tablet_seat_v2_add_listener(
    struct zwp_tablet_seat_v2 *object,
    const struct swl_zwp_tablet_seat_v2_listener_callbacks *callbacks)
{
    return zwp_tablet_seat_v2_add_listener(
        object,
        &swl_zwp_tablet_seat_v2_listener,
        (void *)callbacks);
}

static void swl_zwp_tablet_tool_v2_handle_type(
    void *listener_data,
    struct zwp_tablet_tool_v2 *object,
    uint32_t event_tool_type)
{
    const struct swl_zwp_tablet_tool_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->type) {
        callbacks->type(
            callbacks->data,
            object,
            event_tool_type);
    }
}

static void swl_zwp_tablet_tool_v2_handle_hardware_serial(
    void *listener_data,
    struct zwp_tablet_tool_v2 *object,
    uint32_t event_hardware_serial_hi,
    uint32_t event_hardware_serial_lo)
{
    const struct swl_zwp_tablet_tool_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->hardware_serial) {
        callbacks->hardware_serial(
            callbacks->data,
            object,
            event_hardware_serial_hi,
            event_hardware_serial_lo);
    }
}

static void swl_zwp_tablet_tool_v2_handle_hardware_id_wacom(
    void *listener_data,
    struct zwp_tablet_tool_v2 *object,
    uint32_t event_hardware_id_hi,
    uint32_t event_hardware_id_lo)
{
    const struct swl_zwp_tablet_tool_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->hardware_id_wacom) {
        callbacks->hardware_id_wacom(
            callbacks->data,
            object,
            event_hardware_id_hi,
            event_hardware_id_lo);
    }
}

static void swl_zwp_tablet_tool_v2_handle_capability(
    void *listener_data,
    struct zwp_tablet_tool_v2 *object,
    uint32_t event_capability)
{
    const struct swl_zwp_tablet_tool_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->capability) {
        callbacks->capability(
            callbacks->data,
            object,
            event_capability);
    }
}

static void swl_zwp_tablet_tool_v2_handle_done(
    void *listener_data,
    struct zwp_tablet_tool_v2 *object)
{
    const struct swl_zwp_tablet_tool_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->done) {
        callbacks->done(
            callbacks->data,
            object);
    }
}

static void swl_zwp_tablet_tool_v2_handle_removed(
    void *listener_data,
    struct zwp_tablet_tool_v2 *object)
{
    const struct swl_zwp_tablet_tool_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->removed) {
        callbacks->removed(
            callbacks->data,
            object);
    }
}

static void swl_zwp_tablet_tool_v2_handle_proximity_in(
    void *listener_data,
    struct zwp_tablet_tool_v2 *object,
    uint32_t event_serial,
    struct zwp_tablet_v2 *event_tablet,
    struct wl_surface *event_surface)
{
    const struct swl_zwp_tablet_tool_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->proximity_in) {
        callbacks->proximity_in(
            callbacks->data,
            object,
            event_serial,
            event_tablet,
            event_surface);
    }
}

static void swl_zwp_tablet_tool_v2_handle_proximity_out(
    void *listener_data,
    struct zwp_tablet_tool_v2 *object)
{
    const struct swl_zwp_tablet_tool_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->proximity_out) {
        callbacks->proximity_out(
            callbacks->data,
            object);
    }
}

static void swl_zwp_tablet_tool_v2_handle_down(
    void *listener_data,
    struct zwp_tablet_tool_v2 *object,
    uint32_t event_serial)
{
    const struct swl_zwp_tablet_tool_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->down) {
        callbacks->down(
            callbacks->data,
            object,
            event_serial);
    }
}

static void swl_zwp_tablet_tool_v2_handle_up(
    void *listener_data,
    struct zwp_tablet_tool_v2 *object)
{
    const struct swl_zwp_tablet_tool_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->up) {
        callbacks->up(
            callbacks->data,
            object);
    }
}

static void swl_zwp_tablet_tool_v2_handle_motion(
    void *listener_data,
    struct zwp_tablet_tool_v2 *object,
    wl_fixed_t event_x,
    wl_fixed_t event_y)
{
    const struct swl_zwp_tablet_tool_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->motion) {
        callbacks->motion(
            callbacks->data,
            object,
            event_x,
            event_y);
    }
}

static void swl_zwp_tablet_tool_v2_handle_pressure(
    void *listener_data,
    struct zwp_tablet_tool_v2 *object,
    uint32_t event_pressure)
{
    const struct swl_zwp_tablet_tool_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->pressure) {
        callbacks->pressure(
            callbacks->data,
            object,
            event_pressure);
    }
}

static void swl_zwp_tablet_tool_v2_handle_distance(
    void *listener_data,
    struct zwp_tablet_tool_v2 *object,
    uint32_t event_distance)
{
    const struct swl_zwp_tablet_tool_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->distance) {
        callbacks->distance(
            callbacks->data,
            object,
            event_distance);
    }
}

static void swl_zwp_tablet_tool_v2_handle_tilt(
    void *listener_data,
    struct zwp_tablet_tool_v2 *object,
    wl_fixed_t event_tilt_x,
    wl_fixed_t event_tilt_y)
{
    const struct swl_zwp_tablet_tool_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->tilt) {
        callbacks->tilt(
            callbacks->data,
            object,
            event_tilt_x,
            event_tilt_y);
    }
}

static void swl_zwp_tablet_tool_v2_handle_rotation(
    void *listener_data,
    struct zwp_tablet_tool_v2 *object,
    wl_fixed_t event_degrees)
{
    const struct swl_zwp_tablet_tool_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->rotation) {
        callbacks->rotation(
            callbacks->data,
            object,
            event_degrees);
    }
}

static void swl_zwp_tablet_tool_v2_handle_slider(
    void *listener_data,
    struct zwp_tablet_tool_v2 *object,
    int32_t event_position)
{
    const struct swl_zwp_tablet_tool_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->slider) {
        callbacks->slider(
            callbacks->data,
            object,
            event_position);
    }
}

static void swl_zwp_tablet_tool_v2_handle_wheel(
    void *listener_data,
    struct zwp_tablet_tool_v2 *object,
    wl_fixed_t event_degrees,
    int32_t event_clicks)
{
    const struct swl_zwp_tablet_tool_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->wheel) {
        callbacks->wheel(
            callbacks->data,
            object,
            event_degrees,
            event_clicks);
    }
}

static void swl_zwp_tablet_tool_v2_handle_button(
    void *listener_data,
    struct zwp_tablet_tool_v2 *object,
    uint32_t event_serial,
    uint32_t event_button,
    uint32_t event_state)
{
    const struct swl_zwp_tablet_tool_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->button) {
        callbacks->button(
            callbacks->data,
            object,
            event_serial,
            event_button,
            event_state);
    }
}

static void swl_zwp_tablet_tool_v2_handle_frame(
    void *listener_data,
    struct zwp_tablet_tool_v2 *object,
    uint32_t event_time)
{
    const struct swl_zwp_tablet_tool_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->frame) {
        callbacks->frame(
            callbacks->data,
            object,
            event_time);
    }
}

static const struct zwp_tablet_tool_v2_listener
    swl_zwp_tablet_tool_v2_listener = {
        .type = swl_zwp_tablet_tool_v2_handle_type,
        .hardware_serial = swl_zwp_tablet_tool_v2_handle_hardware_serial,
        .hardware_id_wacom = swl_zwp_tablet_tool_v2_handle_hardware_id_wacom,
        .capability = swl_zwp_tablet_tool_v2_handle_capability,
        .done = swl_zwp_tablet_tool_v2_handle_done,
        .removed = swl_zwp_tablet_tool_v2_handle_removed,
        .proximity_in = swl_zwp_tablet_tool_v2_handle_proximity_in,
        .proximity_out = swl_zwp_tablet_tool_v2_handle_proximity_out,
        .down = swl_zwp_tablet_tool_v2_handle_down,
        .up = swl_zwp_tablet_tool_v2_handle_up,
        .motion = swl_zwp_tablet_tool_v2_handle_motion,
        .pressure = swl_zwp_tablet_tool_v2_handle_pressure,
        .distance = swl_zwp_tablet_tool_v2_handle_distance,
        .tilt = swl_zwp_tablet_tool_v2_handle_tilt,
        .rotation = swl_zwp_tablet_tool_v2_handle_rotation,
        .slider = swl_zwp_tablet_tool_v2_handle_slider,
        .wheel = swl_zwp_tablet_tool_v2_handle_wheel,
        .button = swl_zwp_tablet_tool_v2_handle_button,
        .frame = swl_zwp_tablet_tool_v2_handle_frame,
    };

int swl_zwp_tablet_tool_v2_add_listener(
    struct zwp_tablet_tool_v2 *object,
    const struct swl_zwp_tablet_tool_v2_listener_callbacks *callbacks)
{
    return zwp_tablet_tool_v2_add_listener(
        object,
        &swl_zwp_tablet_tool_v2_listener,
        (void *)callbacks);
}

static void swl_zwp_tablet_v2_handle_name(
    void *listener_data,
    struct zwp_tablet_v2 *object,
    const char *event_name)
{
    const struct swl_zwp_tablet_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->name) {
        callbacks->name(
            callbacks->data,
            object,
            event_name);
    }
}

static void swl_zwp_tablet_v2_handle_id(
    void *listener_data,
    struct zwp_tablet_v2 *object,
    uint32_t event_vid,
    uint32_t event_pid)
{
    const struct swl_zwp_tablet_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->id) {
        callbacks->id(
            callbacks->data,
            object,
            event_vid,
            event_pid);
    }
}

static void swl_zwp_tablet_v2_handle_path(
    void *listener_data,
    struct zwp_tablet_v2 *object,
    const char *event_path)
{
    const struct swl_zwp_tablet_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->path) {
        callbacks->path(
            callbacks->data,
            object,
            event_path);
    }
}

static void swl_zwp_tablet_v2_handle_done(
    void *listener_data,
    struct zwp_tablet_v2 *object)
{
    const struct swl_zwp_tablet_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->done) {
        callbacks->done(
            callbacks->data,
            object);
    }
}

static void swl_zwp_tablet_v2_handle_removed(
    void *listener_data,
    struct zwp_tablet_v2 *object)
{
    const struct swl_zwp_tablet_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->removed) {
        callbacks->removed(
            callbacks->data,
            object);
    }
}

static void swl_zwp_tablet_v2_handle_bustype(
    void *listener_data,
    struct zwp_tablet_v2 *object,
    uint32_t event_bustype)
{
    const struct swl_zwp_tablet_v2_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->bustype) {
        callbacks->bustype(
            callbacks->data,
            object,
            event_bustype);
    }
}

static const struct zwp_tablet_v2_listener
    swl_zwp_tablet_v2_listener = {
        .name = swl_zwp_tablet_v2_handle_name,
        .id = swl_zwp_tablet_v2_handle_id,
        .path = swl_zwp_tablet_v2_handle_path,
        .done = swl_zwp_tablet_v2_handle_done,
        .removed = swl_zwp_tablet_v2_handle_removed,
        .bustype = swl_zwp_tablet_v2_handle_bustype,
    };

int swl_zwp_tablet_v2_add_listener(
    struct zwp_tablet_v2 *object,
    const struct swl_zwp_tablet_v2_listener_callbacks *callbacks)
{
    return zwp_tablet_v2_add_listener(
        object,
        &swl_zwp_tablet_v2_listener,
        (void *)callbacks);
}

static void swl_text_input_v3_handle_enter(
    void *listener_data,
    struct zwp_text_input_v3 *object,
    struct wl_surface *event_surface)
{
    const struct swl_text_input_v3_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->enter) {
        callbacks->enter(
            callbacks->data,
            object,
            event_surface);
    }
}

static void swl_text_input_v3_handle_leave(
    void *listener_data,
    struct zwp_text_input_v3 *object,
    struct wl_surface *event_surface)
{
    const struct swl_text_input_v3_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->leave) {
        callbacks->leave(
            callbacks->data,
            object,
            event_surface);
    }
}

static void swl_text_input_v3_handle_preedit_string(
    void *listener_data,
    struct zwp_text_input_v3 *object,
    const char *event_text,
    int32_t event_cursor_begin,
    int32_t event_cursor_end)
{
    const struct swl_text_input_v3_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->preedit_string) {
        callbacks->preedit_string(
            callbacks->data,
            object,
            event_text,
            event_cursor_begin,
            event_cursor_end);
    }
}

static void swl_text_input_v3_handle_commit_string(
    void *listener_data,
    struct zwp_text_input_v3 *object,
    const char *event_text)
{
    const struct swl_text_input_v3_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->commit_string) {
        callbacks->commit_string(
            callbacks->data,
            object,
            event_text);
    }
}

static void swl_text_input_v3_handle_delete_surrounding_text(
    void *listener_data,
    struct zwp_text_input_v3 *object,
    uint32_t event_before_length,
    uint32_t event_after_length)
{
    const struct swl_text_input_v3_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->delete_surrounding_text) {
        callbacks->delete_surrounding_text(
            callbacks->data,
            object,
            event_before_length,
            event_after_length);
    }
}

static void swl_text_input_v3_handle_done(
    void *listener_data,
    struct zwp_text_input_v3 *object,
    uint32_t event_serial)
{
    const struct swl_text_input_v3_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->done) {
        callbacks->done(
            callbacks->data,
            object,
            event_serial);
    }
}

static void swl_text_input_v3_handle_action(
    void *listener_data,
    struct zwp_text_input_v3 *object,
    uint32_t event_action,
    uint32_t event_serial)
{
    const struct swl_text_input_v3_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->action) {
        callbacks->action(
            callbacks->data,
            object,
            event_action,
            event_serial);
    }
}

static void swl_text_input_v3_handle_language(
    void *listener_data,
    struct zwp_text_input_v3 *object,
    const char *event_language)
{
    const struct swl_text_input_v3_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->language) {
        callbacks->language(
            callbacks->data,
            object,
            event_language);
    }
}

static void swl_text_input_v3_handle_preedit_hint(
    void *listener_data,
    struct zwp_text_input_v3 *object,
    uint32_t event_start,
    uint32_t event_end,
    uint32_t event_hint)
{
    const struct swl_text_input_v3_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->preedit_hint) {
        callbacks->preedit_hint(
            callbacks->data,
            object,
            event_start,
            event_end,
            event_hint);
    }
}

static const struct zwp_text_input_v3_listener
    swl_text_input_v3_listener = {
        .enter = swl_text_input_v3_handle_enter,
        .leave = swl_text_input_v3_handle_leave,
        .preedit_string = swl_text_input_v3_handle_preedit_string,
        .commit_string = swl_text_input_v3_handle_commit_string,
        .delete_surrounding_text = swl_text_input_v3_handle_delete_surrounding_text,
        .done = swl_text_input_v3_handle_done,
        .action = swl_text_input_v3_handle_action,
        .language = swl_text_input_v3_handle_language,
        .preedit_hint = swl_text_input_v3_handle_preedit_hint,
    };

int swl_text_input_v3_add_listener(
    struct zwp_text_input_v3 *object,
    const struct swl_text_input_v3_listener_callbacks *callbacks)
{
    return zwp_text_input_v3_add_listener(
        object,
        &swl_text_input_v3_listener,
        (void *)callbacks);
}

static void swl_zxdg_output_v1_handle_logical_position(
    void *listener_data,
    struct zxdg_output_v1 *object,
    int32_t event_x,
    int32_t event_y)
{
    const struct swl_zxdg_output_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->logical_position) {
        callbacks->logical_position(
            callbacks->data,
            object,
            event_x,
            event_y);
    }
}

static void swl_zxdg_output_v1_handle_logical_size(
    void *listener_data,
    struct zxdg_output_v1 *object,
    int32_t event_width,
    int32_t event_height)
{
    const struct swl_zxdg_output_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->logical_size) {
        callbacks->logical_size(
            callbacks->data,
            object,
            event_width,
            event_height);
    }
}

static void swl_zxdg_output_v1_handle_done(
    void *listener_data,
    struct zxdg_output_v1 *object)
{
    const struct swl_zxdg_output_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->done) {
        callbacks->done(
            callbacks->data,
            object);
    }
}

static void swl_zxdg_output_v1_handle_name(
    void *listener_data,
    struct zxdg_output_v1 *object,
    const char *event_name)
{
    const struct swl_zxdg_output_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->name) {
        callbacks->name(
            callbacks->data,
            object,
            event_name);
    }
}

static void swl_zxdg_output_v1_handle_description(
    void *listener_data,
    struct zxdg_output_v1 *object,
    const char *event_description)
{
    const struct swl_zxdg_output_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->description) {
        callbacks->description(
            callbacks->data,
            object,
            event_description);
    }
}

static const struct zxdg_output_v1_listener
    swl_zxdg_output_v1_listener = {
        .logical_position = swl_zxdg_output_v1_handle_logical_position,
        .logical_size = swl_zxdg_output_v1_handle_logical_size,
        .done = swl_zxdg_output_v1_handle_done,
        .name = swl_zxdg_output_v1_handle_name,
        .description = swl_zxdg_output_v1_handle_description,
    };

int swl_zxdg_output_v1_add_listener(
    struct zxdg_output_v1 *object,
    const struct swl_zxdg_output_v1_listener_callbacks *callbacks)
{
    return zxdg_output_v1_add_listener(
        object,
        &swl_zxdg_output_v1_listener,
        (void *)callbacks);
}

static void swl_zxdg_toplevel_decoration_v1_handle_configure(
    void *listener_data,
    struct zxdg_toplevel_decoration_v1 *object,
    uint32_t event_mode)
{
    const struct swl_zxdg_toplevel_decoration_v1_listener_callbacks *callbacks =
        listener_data;
    if (callbacks && callbacks->configure) {
        callbacks->configure(
            callbacks->data,
            object,
            event_mode);
    }
}

static const struct zxdg_toplevel_decoration_v1_listener
    swl_zxdg_toplevel_decoration_v1_listener = {
        .configure = swl_zxdg_toplevel_decoration_v1_handle_configure,
    };

int swl_zxdg_toplevel_decoration_v1_add_listener(
    struct zxdg_toplevel_decoration_v1 *object,
    const struct swl_zxdg_toplevel_decoration_v1_listener_callbacks *callbacks)
{
    return zxdg_toplevel_decoration_v1_add_listener(
        object,
        &swl_zxdg_toplevel_decoration_v1_listener,
        (void *)callbacks);
}
