# Protocol Patches

Local protocol XML patches belong here only when SwiftWayland must carry a documented deviation from upstream XML.

The default policy is no local patches. Prefer updating `protocols/upstream/` from the upstream source package and recording the copied file in `protocols/manifest.json`.
