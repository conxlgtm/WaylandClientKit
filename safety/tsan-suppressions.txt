# Swift 6.3.2 runtime metadata cache can report lock-order and race warnings
# under ThreadSanitizer before package tests start. These suppressions are
# limited to Swift runtime metadata-cache frames so project races still fail.
mutex:swift::VariadicMetadataCacheEntryBase
mutex:swift::StableAddressConcurrentReadableHashMap
race:swift::MetadataCacheKey::operator==
race:swift::VariadicMetadataCacheEntryBase
race:swift_getWitnessTable

# Swift Testing 6.3.2 can race while recording human-readable event graph output
# under TSan. Keep this scoped to the Testing.Graph value witness/subgraph path.
race:$s7Testing5Graph
race:libTesting.so

# Swift 6.3.2 libdispatch can report its continuation allocator's calloc/free
# reuse as a race on hosted Ubuntu. Keep this scoped to the allocator frame;
# the observed access stacks contain no package code.
race:_dispatch_continuation_alloc_from_heap
